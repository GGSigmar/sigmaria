{% extends "base/base.html.twig" %}
{% block content %}
    <h1>Weapon Damage</h1>

    <div class="col-md-12 mb-2 text-right">
        <a href="{{ path('create_weapon_damage') }}" class="btn btn-outline-success" role="button">New</a>
    </div>

    <table class="table table-striped bg-light text-nowrap text-center">
        <thead>
            <tr>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for weaponDamage in weaponDamages %}
                <tr>
                    <td>{{ weaponDamage.name }}</td>
                    <td>
                        <a href="{{ path('edit_weapon_damage', {"id":weaponDamage.id}) }}" class="btn btn-outline-primary" role="button">Edit</a>
                        <a href="{{ path('delete_weapon_damage', {"id":weaponDamage.id}) }}" class="btn btn-outline-danger" role="button">Delete</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
