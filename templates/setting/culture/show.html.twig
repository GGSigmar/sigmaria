{% extends "base/base.html.twig" %}

{% block content %}
    <div class="col-md-12">
        {% include "base/utilities/entity_show_header.html.twig" with {'entity': culture} %}
    </div>
    <div class="col-md-9 pt-2 pb-2">
        {% include 'core/paragraph/paragraph_entity_display.html.twig' with {'entity': culture} %}
    </div>
    <div class="col-md-3 sidebar sidebar-right color-blood text-justify pt-2 pb-2">
        <h3>Pospolite rasy</h3>
        <p class="mb-4">
            {% if culture.commonAncestries is empty %}
                Brak.
            {% else %}
                {% for ancestry in culture.commonAncestries %}
                    {% include "base/badges/entity_show_badge.html.twig" with {'entityName': 'ancestry', 'entityId': ancestry.id} %} {{ ancestry.name }}
                    {% if not loop.last %}
                        <br />
                    {% endif %}
                {% endfor %}
            {% endif %}
        </p>
        <h3>Pospolite klasy</h3>
        <p class="mb-4">
            {% if culture.commonClasses is empty %}
                Brak.
            {% else %}
                {% for class in culture.commonClasses %}
                    {{ class.name }}

                    {% if not loop.last %}
                        <br />
                    {% endif %}
                {% endfor %}
            {% endif %}
        </p>
        <h3>Pospolite pochodzenia</h3>
        <p class="mb-4">
            {% if culture.commonBackgrounds is empty %}
                Brak.
            {% else %}
                {% for background in culture.commonBackgrounds %}
                    {% include "base/badges/entity_show_badge.html.twig" with {'entityName': 'background', 'entityId': background.id} %} {{ background.name }}

                    {% if not loop.last %}
                        <br />
                    {% endif %}
                {% endfor %}
            {% endif %}
        </p>
        <h3>Pospolite języki</h3>
        <p class="mb-4">
            {% if culture.commonLanguages is empty %}
                Brak.
            {% else %}
                {% for language in culture.commonLanguages %}
                    {% include "base/badges/entity_show_badge.html.twig" with {'entityName': 'language', 'entityId': language.id} %} {{ language.name }}

                    {% if not loop.last %}
                        <br />
                    {% endif %}
                {% endfor %}
            {% endif %}
        </p>
    </div>

    {% include 'core/feat/entity_display.html.twig' with {'displayTitle': 'Atuty kulturowe', 'entity': culture} %}
{% endblock %}