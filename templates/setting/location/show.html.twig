{% extends "base/base.html.twig" %}

{% block content %}
    <div class="col-md-12">
        {% include "base/utilities/entity_show_header.html.twig" with {'entity': location} %}
    </div>
    <div class="col-md-6">
        <h3>
            Rzadkość
        </h3>
        <p class="mb-4">{{ language.rarity.name }}</p>
    </div>
    <div class="col-md-6">
        <h3>
            Pismo
        </h3>
        <p class="mb-4">{{ language.script ? language.script.name : 'Nie dotyczy' }}</p>
    </div>
    <div class="col-md-12">
        {{ language.description|raw }}
    </div>
{% endblock %}