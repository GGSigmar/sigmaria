{% if isAdmin %}
    {% if not entity.isActive %}
        {% if entity.isEdit is defined and entity.isEdit %}
            bg-secondary
        {% elseif entity.isToBeReleased %}
            bg-success
        {% else %}
            bg-danger
        {% endif %}
    {% endif %}
{% endif %}