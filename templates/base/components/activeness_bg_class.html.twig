{% if isAdmin %}
    {% if not entity.isActive %}
        {% if entity.isToBeReleased %}
            bg-success
        {% else %}
            bg-danger
        {% endif %}
    {% endif %}
{% endif %}