{% extends "base/base.html.twig" %}
{% block content %}
    <h1>Stwórz rasę</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Zapisz</button>
    {{ form(form) }}
{% endblock %}
